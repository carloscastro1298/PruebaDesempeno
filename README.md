# Performance Test
This is a Python program that allows you to register, query, search, update, and delete student information by ID.
It works with dictionaries, tuples, and lists.

It can also be imported into a JSON file.
